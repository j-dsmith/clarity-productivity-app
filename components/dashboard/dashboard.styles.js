import styled from 'styled-components';
import { motion } from 'framer-motion';

export const StyledDashContainer = styled(motion.div)`
  padding: 1rem;
  height: 50%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  text-shadow: 0.5px 0.5px 3px rgba(0, 0, 0, 0.4);
`;
